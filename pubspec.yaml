name: several_social_logins
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.0-142.0.dev <3.0.0'

dependencies:
  cupertino_icons: ^1.0.2
  firebase_auth: ^3.3.19
  flutter:
    sdk: flutter
  google_fonts: ^3.0.1
  provider: ^6.0.3

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  assets:
    - assets/images/
